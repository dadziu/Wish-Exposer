jwt:
  secret: '[a-zA-Z]^+$XYZxyz$'

server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wishexposer?useLegacyDatetimeCode=false&serverTimezone=Europe/Warsaw&useSSL=false&useUnicode=true&characterEncoding=utf8
    password: root
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect